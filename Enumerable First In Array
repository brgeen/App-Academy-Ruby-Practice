def first_in_array(arr, el1, el2)

  arr.each do |ele|
    if ele.include?(el1) || ele.include?(el2)
      return ele
    end
  end
  
end

puts first_in_array(["a", "b", "c", "d"], "d", "b"); # => "b"
puts first_in_array(["cat", "bird" ,"dog", "mouse" ], "dog", "mouse"); # => "dog"
